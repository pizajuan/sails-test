/**
 * PolicyController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */

module.exports = {
  

  /**
   * `PolicyController.isAuthorized()`
   */
  isAuthorized: async function (req, res) {
    return res.json({
      todo: 'isAuthorized() is not implemented yet!'
    });
  }

};

